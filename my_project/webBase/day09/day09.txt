复习 :
1. 运算符
	关系运算符 
	逻辑运算符 && || !
	三目运算符 表达式1?表达式2:表达式3
2. 流程控制
	1. 分支结构
		if语句
			if(){
			
			}else if(){
			
			}else if(){
			
			}else{
			
			}
		switch语句
			switch(){
				case 值1:
					匹配全等时执行;
					break;
				case 值2:
					//
					break;
				default:
					默认执行的操作
				break;
			}
			1. 省略break,会从匹配到的case开始向后执行所有的代码
			2. 共用代码段
					case 1:
					case 2:
						//上述case只要匹配到,都会执行
	2. 循环结构
		1. while 循环
			while(){
			
			}
		2. do-while循环
			do{
			
			}while();
			不管条件是否成立,先执行一次循环体
		3. for循环
			for(var i = 1; i < 101; i++){
				
			}
-----------------------------------------

day 09
1. 函数
	作用 : 封装一段待执行的代码
	语法 :
		函数声明
			function 函数名(参数列表){
				函数体
				return 返回值;
			}
		函数调用
			函数名(参数列表);
	使用 :
		详细见01-function.html
		函数名自定义,见名知意,命名规范参照变量的命名规范,普通函数以小写字母开头,用于区分构造函数(构造函数使用大写字母开头,定义类)
	匿名函数 :
		1. 匿名函数自执行
			(function (形参){
			
			
			})(实参);
		2. 定义变量接收匿名函数
			var fn = function (){};
			fn(); //函数调用

	----------------
	练习 :
		使用函数封装日期计算器
		1. 主体功能封装函数,通过按钮的点击事件调用
				onclick="f1();"
		2. 闰年判断封装成函数
2. 作用域
	JS中作用域分为全局作用域和函数作用域
	以函数的{}作为划分作用域的依据
	1. 全局变量和全局函数
		1. 只要在函数外部使用var关键字定义的变量,或函数都是全局变量和全局函数,在任何地方都可以访问
		2. 所有省略var关键字定义的变量,一律是全局变量
	2. 局部变量/局部函数
		在函数内部使用var关键字定义的变量为局部变量,函数内部定义的函数也为局部函数,只能在当前作用域中使用,外界无法访问
	3. 作用域链
		局部作用域中访问变量或函数,首先从当前作用域中查找,当前作用域中没有的话,向上级作用域中查找,直至全局作用域
3. 内置对象
	1. 对象是由属性和方法组成的,使用点语法访问
	2. Array 数组
		创建 :
			详细见04-array.html
			特殊情况 : 使用new Array()创建数组时,如果只给一个整数值,表示预分配数组长度.
		特点 :
			1. 数组用于存储若干数据,自动为每位数据分配下标,从0开始
			2. 数组中的元素不限数据类型,长度可以动态调整
		动态操作数组元素 :
			根据元素下标读取或修改数组元素
			arr[index]
			见04-array.html
		遍历数组 :
			见05-遍历数组.html
			练习 :
				1. 循环接收用户输入,将数据存储到数组中,直至用户输入"exit"结束循环录入,输出数组
				2. 声明包含number值的数组,求数组中元素的最大值
				3. 声明包含3个元素的数组,比较并排序,按照从小到大的顺序排列后输出
				4. 自定义数据源,接收用户输入,查询数据在数组中的对应下标并输出.查找失败返回-1
		属性和方法
			属性 : length 表示数组长度,可读可写
			方法 :
				1. push(a,b,c)
					在数组末尾添加元素,多个元素之间使用逗号隔开
					返回数组最新的长度
				2. pop()
					移除数组的末尾元素
					返回被删除的元素
				3. unshift(a,b,c)
					在数组头部添加元素
					返回添加后的数组长度
				4. shift()
					移除数组头部元素
					返回被移除的元素
					作业 :
						1. 使用数组方法实现循环录入存储数据
						2. 十进制转换二进制	
							除2取余,直至商为0,余数倒序排列
							例 :
								6 / 2 = 3 ... 0
								3 / 2 = 1 ... 1
								1 / 2 = 0 ... 1


				





			
















		
			





